# Handler
An AI-powered email triage tool that automatically reads incoming emails for freight/logistics companies and classifies them into actionable categories — quote requests, shipment status, booking requests, document requests, or unrelated — so operations teams can respond faster without manually sorting their inbox.
